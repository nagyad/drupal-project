parameters:
  hello.world.cache: true

services:

  # Defines a simple service of which requires no parameter for its constructor.
  hello_world.world_generator:
    class: Drupal\hello_world\World\WorldGenerator
    arguments:
      - '@keyvalue'
      - %hello.world.cache%